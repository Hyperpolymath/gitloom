project Badge is
   for Source_Dirs use ("src");
   for Object_Dir use "../obj/badge";
   for Exec_Dir use "../bin";
   for Main use ("badge.adb");
   package Compiler is for Default_Switches ("Ada") use ("-g", "-O2", "-gnatwa"); end Compiler;
end Badge;
